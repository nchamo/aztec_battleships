{
  "name": "battleships",
  "private": true,
  "workspaces": [
    "web",
    "contracts"
  ],
  "scripts": {
    "dev": "yarn workspace battleships-web dev",
    "build": "yarn workspace battleships-contracts codegen && yarn workspace battleships-web build",
    "test": "yarn workspace battleships-contracts test",
    "clean": "yarn workspace battleships-web clean && yarn workspace battleships-contracts clean"
  }
}
